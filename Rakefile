#!/usr/bin/env rake
# Add your own tasks in files placed in lib/tasks ending in .rb,
# for example lib/tasks/capistrano.rb, and they will automatically be available to Rake.

require File.expand_path('../lib/application', __FILE__)

Application.load_tasks
